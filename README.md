# SQL
A structured collection of SQL scripts for multiple relational databases such as MSSQL and PostgreSQL. Includes schema definitions, sample datasets, functions, queries, and utility scripts, all organized by database type for clarity and reusability.
